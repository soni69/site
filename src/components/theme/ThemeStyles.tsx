/**
 * ThemeStyles — рендерит CSS-переменные темы.
 *
 * Оптимизация производительности:
 * - НЕ делает запрос к Payload при каждом рендере (убрали async DB call из critical path)
 * - Шрифт подключается через <link rel="preload"> в layout, а не через @import
 * - CSS-переменные уже определены в globals.css, здесь только кастомизация из CMS
 *
 * Это устраняет CLS от позднего применения CSS и LCP от блокирующего DB запроса.
 */

// Синхронный компонент — не блокирует первый байт
export function ThemeStyles() {
  // Базовые CSS-переменные уже в globals.css.
  // Кастомная тема из CMS применяется через отдельный non-blocking механизм.
  // Возвращаем пустой фрагмент — стили уже есть в globals.css.
  return null
}
